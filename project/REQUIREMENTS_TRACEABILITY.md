# Initial requirements traceability

This is the initial audit slice. “Observed” means verified in the local checkout; “gap” means not found in the inspected code, not a claim that no remote work exists.

| Requirement | Design evidence | Current implementation evidence | Gap / contradiction | Initial ticket | Verification target |
|---|---|---|---|---|---|
| OTP auth with expiry, attempts, session invalidation | LLD §5.1, §7.1; `auth_otp_verified.svg` | Backend users routes, Redis/session middleware, OTP tests; router registers users | Integration coverage and end-to-end evidence are limited | AUTH-001 | API integration tests + seeded demo login |
| Onboarding and presigned uploads | LLD §5.1/§5.8; `onboarding.svg`, `presigned_upload.svg` | Backend onboarding/upload handlers and migrations are wired | Provider callbacks, admin review, and full persona evidence incomplete | AUTH-002 | upload/mock callback and rejection/resubmit tests |
| Profile and address CRUD | LLD §5.1; `profile.svg`, `address_crud.svg` | Backend routes/business/repository code and migrations exist | No browser journey evidence; schema/contract drift must be checked | CUST-001 | API + browser journey, IDOR/soft-delete tests |
| Restaurant discovery and menu | LLD §5.2; `7f30730` adds public discovery/detail/menu routes, PostGIS radius query, cursor pagination, minor-unit menu output, repeatable Shamgarh seed, and owner-scoped item create/update/soft-delete with audit logs | Redis discovery cache and browser persona evidence are deferred | VERT-001 | unit/race tests, live PostGIS HTTP checks, owner mutation/audit smoke test, two seed runs, and independent approval passed |
| Cart | LLD §5.3; `7f30730` wires authenticated cart routes, HMAC tokens, atomic Postgres persistence, restaurant/item checks, customisations, expiry enforcement, and minor-unit subtotal reads | Frontend integration remains pending | VERT-002 | unit/race tests, cross-user IDOR check, forged-token/mismatch negatives, live create/read/clear flow, and independent approval passed |
| Place order and server-side quote | LLD §5.4; `place_order.svg`; DB §9 APIs 29–30 | Backend commit `7f30730` adds quote/place routes, address ownership/serviceability, menu price revalidation, INR minor-unit totals, idempotency replay, order-item snapshots, customisations, cart conversion, and consistent transaction lock ordering | Order-service gRPC integration and get/list/cancel lifecycle remain separate follow-up work; unique idempotency index is not used because orders are partitioned | ORDER-001, DATA-001 | business/handler/race tests, live quote/place/replay/customisation flow, and independent approval passed |
| Payment adapter and verification | LLD §5.5; `payment.svg` | Backend commit `df1a960` adds `PAYMENT_PROVIDER=mock`, migration 27, authenticated `POST /api/v1/orders/:orderId/payment`, idempotent persisted success/decline outcomes, and explicit startup failure for unsupported real mode | No live commercial provider; frontend checkout caller remains pending | ORDER-002 | mock failure/success, replay/ownership tests, migration, Docker route registration, and CI passed |
| Backend-controlled 600s simulated delivery | Master prompt §10; LLD §5.7; assignment/tracking SVGs | Backend commit `7f1ddab` adds mock provider config, persisted due timestamps, seeded fictional drivers, restart-safe worker, order-status progression, and customer delivery status route; frontend PR #3 adds authenticated polling timeline at `/orders/:orderId/tracking` | No live courier/GPS claim; browser tracking evidence and optional wall-clock 10-minute run remain | DELIVERY-001 | migration/live due-time progression and frontend CI passed; browser journey and optional wall-clock run remain |
| Notifications | LLD §5.6; `notifications.svg` | Backend `71e6cf5` adds recipient-scoped list/read/read-all APIs, mock order/payment outcome events, and operations failure/stall counters; frontend `30d99a3` includes the notification center and reconciliation metrics; repeatable demo fixtures are seeded | NATS consumer delivery and external email/SMS remain intentionally out of scope; delivery failure is represented as a due/stalled reconciliation signal | OPS-001 | API ownership/negative tests, notification read-state tests, order/payment integration, direct SQL counters, Docker checks, and CI |
| Customer/restaurant/driver/admin journeys | LLD §2; frontend source tree | Customer flow covers discovery, menu, cart, checkout, mock payment, and tracking; owner queue and driver workflow are role guarded; backend `c6eb552` and frontend `1f67d5f` add manager-only operations overview, driver coverage, status filtering, and audited active-order cancellation | Browser evidence remains; existing `restaurant_manager` role is used as the demo operations role because no separate admin enum exists | FRONT-001, PERSONA-001 | role-specific API/UI checks, audited intervention tests, and responsive browser persona matrix |
| Restaurant owner order operations | LLD §2; order/restaurant flows | Backend `GET /api/v1/owner/restaurant`, `GET /api/v1/restaurants/:restaurantId/orders`, and `PATCH /api/v1/restaurants/:restaurantId/orders/:orderId/status` enforce owner scope; frontend queue exposes valid accept, prepare, ready, and reject actions | Browser persona evidence remains; driver pickup handoff is next | PERSONA-001 | owner positive/non-owner negative API flow and responsive owner queue |
| Driver availability and pickup workflow | LLD §2; driver/assignment/tracking flows | Mock delivery assignment excludes busy drivers; driver-only `GET /api/v1/driver/delivery` and `PATCH /api/v1/driver/delivery/status` persist `updated_by`, order status, delivery history, and due-time fallback; frontend `/driver` controls availability and each valid progression; operations workspace lists driver availability and active assignments | Browser persona evidence remains | PERSONA-001 | driver positive/non-driver negative API flow, transition tests, and responsive driver dashboard |
| Customer order history and cancellation | LLD §2; order lifecycle/history flows | Backend `b1f90cb` adds recipient-scoped `GET /api/v1/orders`, `GET /api/v1/orders/:orderId/history`, and guarded `PATCH /api/v1/orders/:orderId/cancel`; frontend `5134e71` adds `/orders/history` with timeline and cancellation action | Browser evidence remains; cancellation is limited to active mock orders | ORDER-003 | ownership, terminal-state, audit, delivery cleanup, notification, and CI checks |
| Customer order history and cancellation | LLD §2; order lifecycle/history flows | Backend `b1f90cb` adds recipient-scoped `GET /api/v1/orders`, `GET /api/v1/orders/:orderId/history`, and guarded `PATCH /api/v1/orders/:orderId/cancel`; frontend `5134e71` adds `/orders/history` with timeline and cancellation action | Browser evidence remains; cancellation is limited to active mock orders | ORDER-003 | ownership, terminal-state, audit, delivery cleanup, notification, and CI checks |
| Database design alignment | DB design §§1–10; `er_diagram.svg` | Backend has 22 up migrations | Current migrations use several VARCHAR/TEXT fields and reduced columns vs DB v2.0; exact compatibility plan missing | DATA-001 | schema diff + reviewed forward migration |
| CI/development infrastructure | Master prompt §11; infra README | Backend/frontend workflows and Terraform exist; Terraform validates | Secrets, reachable deployment, backup/restore, monitoring and cost evidence not verified | INFRA-001 | CI artifacts + dev environment checklist |
